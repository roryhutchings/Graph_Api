//List of loaders for Locations' data in order to lower the count of queries to the database, solving N+1 Problem
const DataLoader=require('dataloader')

// TODO: Create Locations loader for locationid
